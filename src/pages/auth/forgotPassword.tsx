import type { NextPage } from "next";

const ForgotPassword: NextPage = () => {
   return (
      <div>
         <header>
            <h1>Forgot your password?</h1>
            <p>Its ok, lest recover them!</p>
         </header>
      </div>
   );
};

export default ForgotPassword;
